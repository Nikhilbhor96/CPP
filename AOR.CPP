#include<iostream.h>
#include<conio.h>
int main ()
{
int length, width, area;
cout<<"Enter length & width of rectangle";
cin>>length>>width;
area=length*width;
cout<<"area of rectangle is "<<area;
getch();
return 0;
}